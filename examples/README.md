# Examples

One notebook per usage case, each linked from the top-level README.

| notebook | shows |
|---|---|
| `scalar.ipynb` | a normal operator from a trajectory, against `A_adjoint(A(x))` |
| `subspace.ipynb` | a subspace normal, and what grouping frames onto shared trajectories saves |
| `cartesian.ipynb` | the Cartesian subspace normal, which needs no gridding |
| `coil_kernels.ipynb` | sensitivities as k-space kernels, and `truncation_error` |
| `streaming.ipynb` | a transfer larger than the card, staged from pinned host memory |
| `unrolled.ipynb` | gradients through the normal inside an unrolled network |

Not yet written.
